import React from 'react';
import './Buttons.css'

const buttons = (props) => {
    return (
        <div className='control'>
        <button>Raw</button>
        </div>

    )
    
}

export default buttons;