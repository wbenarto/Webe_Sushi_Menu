import React from 'react';
import './Buttons.css'

const buttons = (props) => {
    return (
        <div className='control'>
        <button>Buttons Comp</button>
        </div>

    )
    
}

export default buttons;